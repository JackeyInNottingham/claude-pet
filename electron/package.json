{
  "name": "claude-pet-electron",
  "version": "0.2.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "sync": "node ../scripts/sync-version.js",
    "start": "npm run sync --silent && electron .",
    "demo": "npm run sync --silent && electron demo-main.js"
  },
  "dependencies": {
    "electron": "^33.2.1"
  }
}
